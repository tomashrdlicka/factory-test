{
  "name": "taskflow",
  "version": "0.1.0",
  "description": "A minimal task management CLI - built by Smith factory loop",
  "type": "module",
  "scripts": {
    "test": "node --test src/**/*.test.js",
    "verify": "node --test src/**/*.test.js",
    "lint": "echo 'lint ok'"
  }
}
